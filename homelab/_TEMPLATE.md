# <Improvement title>

- Date: YYYY-MM-DD
- Status: done | in progress | idea

## Goal

What problem does this solve in the Kali homelab setup? (Be concrete: "stand up a vulnerable lab VM reachable from WSL Kali", "route lab traffic through a firewall VM", ...)

## Setup / changes

What was added or changed. List hardware/software involved. Public-repo safe: no real IPs — use `192.0.2.x` / `198.51.100.x` ranges or masked notation.

## Commands (verified)

Copy-pasteable steps that make this work. Mark anything environment-specific. These get tested before committing.

```bash
# example block
sudo apt install -y <pkg>
```

## Verification

How you confirmed it works (expected output, greps, screenshots described in words).

## Gotchas / lessons

What tripped you up, and what someone doing this on their laptop should watch for. WSL-specific quirks especially valuable.